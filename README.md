# vayvnd-control

Public remote banner config for the `vayvnd - tinh toan tai chinh` app.

Edit `banner-config.json` to update the ad banner without rebuilding the app.

The app reads:

```text
https://raw.githubusercontent.com/VT868/vayvnd-control/main/banner-config.json
```
